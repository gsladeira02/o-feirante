import { NextResponse } from 'next/server';import { getCurrentProfile } from '@/lib/current';import { createAdminSupabase } from '@/lib/supabase/admin';
export async function POST(req:Request){const {profile}=await getCurrentProfile(); const f=await req.formData(); const admin=createAdminSupabase(); const email=String(f.get('email')||''); const password=String(f.get('password')||''); const {data,error}=await admin.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{full_name:f.get('full_name')}}); if(error) return NextResponse.json({error:error.message},{status:400}); await admin.from('profiles').insert({auth_user_id:data.user?.id,law_firm_id:profile.law_firm_id,full_name:f.get('full_name'),email,phone:f.get('phone'),role:'membro',oab_number:f.get('oab_number'),status:'ativo'}); return NextResponse.redirect(new URL('/app/usuarios',req.url),303)}
