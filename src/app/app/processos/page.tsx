import { PageHeader } from '@/components/PageHeader';import { getCurrentProfile } from '@/lib/current';import { money } from '@/lib/utils';
export default async function Processos(){const {supabase,profile}=await getCurrentProfile(); const [cases,clients]=await Promise.all([supabase.from('cases').select('*, clients(name)').eq('law_firm_id',profile.law_firm_id).order('created_at',{ascending:false}),supabase.from('clients').select('id,name').eq('law_firm_id',profile.law_firm_id)]); return <div><PageHeader title="Processos" subtitle="Controle de processos e fases."/><section className="card mb-6 p-5"><form action="/api/cases" method="post" className="grid gap-4 md:grid-cols-4"><select className="input" name="client_id"><option value="">Cliente</option>{(clients.data||[]).map((c:any)=><option value={c.id} key={c.id}>{c.name}</option>)}</select><input className="input" name="case_number" placeholder="Nº processo"/><select className="input" name="area"><option>cível</option><option>trabalhista</option><option>família</option><option>previdenciário</option><option>consumidor</option><option>criminal</option><option>empresarial</option><option>tributário</option><option>imobiliário</option></select><input className="input" name="action_type" placeholder="Tipo de ação"/><input className="input" name="court" placeholder="Tribunal/Vara"/><input className="input" name="district" placeholder="Comarca"/><input className="input" name="opposing_party" placeholder="Parte contrária"/><input className="input" name="responsible" placeholder="Responsável"/><input className="input" name="phase" placeholder="Fase atual"/><select className="input" name="status"><option>ativo</option><option>suspenso</option><option>arquivado</option><option>encerrado</option></select><input className="input" name="claim_value" placeholder="Valor da causa" type="number" step="0.01"/><input className="input" name="notes" placeholder="Observações"/><button className="btn btn-primary md:col-span-4">Cadastrar processo</button></form></section><table className="table"><thead><tr><th>Processo</th><th>Cliente</th><th>Área</th><th>Status</th><th>Valor</th></tr></thead><tbody>{(cases.data||[]).map((c:any)=><tr key={c.id}><td><b>{c.case_number||'Sem número'}</b><p className="text-sm text-slate-500">{c.action_type}</p></td><td>{c.clients?.name}</td><td>{c.area}</td><td><span className="badge badge-info">{c.status}</span></td><td>{money(c.claim_value)}</td></tr>)}</tbody></table></div>}
