import { PageHeader } from '@/components/PageHeader';import { getCurrentProfile } from '@/lib/current';import { dateBR, deadlineClass } from '@/lib/utils';
export default async function Prazos(){const {supabase,profile}=await getCurrentProfile(); const [deadlines,clients,cases]=await Promise.all([supabase.from('deadlines').select('*, clients(name), cases(case_number)').eq('law_firm_id',profile.law_firm_id).order('due_date'),supabase.from('clients').select('id,name').eq('law_firm_id',profile.law_firm_id),supabase.from('cases').select('id,case_number').eq('law_firm_id',profile.law_firm_id)]); return <div><PageHeader title="Prazos" subtitle="Acompanhe vencimentos e prioridades."/><section className="card mb-6 p-5"><form action="/api/deadlines" method="post" className="grid gap-4 md:grid-cols-4"><input className="input" name="title" placeholder="Título do prazo" required/><input className="input" name="due_date" type="date" required/><select className="input" name="client_id"><option value="">Cliente</option>{(clients.data||[]).map((c:any)=><option value={c.id} key={c.id}>{c.name}</option>)}</select><select className="input" name="case_id"><option value="">Processo</option>{(cases.data||[]).map((c:any)=><option value={c.id} key={c.id}>{c.case_number}</option>)}</select><input className="input" name="responsible" placeholder="Responsável"/><select className="input" name="priority"><option>normal</option><option>alta</option><option>urgente</option></select><select className="input" name="status"><option>pendente</option><option>em andamento</option><option>concluido</option></select><input className="input" name="description" placeholder="Descrição"/><button className="btn btn-primary md:col-span-4">Cadastrar prazo</button></form></section><table className="table"><thead><tr><th>Prazo</th><th>Data</th><th>Cliente</th><th>Processo</th><th>Status</th></tr></thead><tbody>{(deadlines.data||[]).map((d:any)=><tr key={d.id}><td><b>{d.title}</b><p className="text-sm text-slate-500">{d.priority}</p></td><td>{dateBR(d.due_date)}</td><td>{d.clients?.name}</td><td>{d.cases?.case_number}</td><td><span className={'badge '+deadlineClass(d.due_date,d.status)}>{d.status}</span></td></tr>)}</tbody></table></div>}
